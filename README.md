# Library Management System

A small, classic PHP/MySQL web app to manage books, students, borrowings, fines and basic reports. The UX is simple — built with plain PHP, server-side logic in the `crud/` folder, and a small API under `crud/api/`.

- Main entry: `index.php`
- Database dump: `library_db (1).sql`

- this system provide a simple and basic operation of the process of the library it can be further enchance, this system introduce a differ understanding of the crud it serve us to find otehr way and think beyond what the teacher introduce.  




